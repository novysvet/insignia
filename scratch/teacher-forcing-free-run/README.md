# Teacher forcing to free run: reproduced outputs

Generated by:

```bash
python tools/evaluate_teacher_forcing_free_run.py
```

Files:

- `summary.json`: machine-readable headline results and gate recommendation.
- `exact-path-law.json`: exact rational `P^T` and `Q^T` for a materialized
  two-token, four-step model, with direct and chain-rule KL.
- `exact-cascades.json`: rational rare-history cascades at horizons 50 and 256.
- `adversary-search.csv`: best rational finite-state pairs found under total
  exact-policy trajectory-KL budget 0.5.
- `zero-failure-confidence-widths.csv`: 95% Jeffreys-mixture anytime UCBs.
- `confidence-coverage.csv`: 5,000-run optional-stopping checks per scenario.
- `ppl-reallocation-counterexample.json`: zero forced-token PPL delta with a
  large one-step sampling-failure change.
- `corpus-coverage-hole.json`: zero corpus local KL with 99% deployment failure
  when the deployment-to-corpus density cap is infinite.
- `greedy-metric-counterexample.json`: an argmax flip with cosine rounded to one
  and MSE below `1e-12`.
